"use strict";
const medicaments = require("./nomonclature.json");
/* const fs = require("fs");

let rawdata = fs.readFileSync("./nomonclature.json");
let medicaments = JSON.parse(rawdata);
 */
module.exports = medicaments;
