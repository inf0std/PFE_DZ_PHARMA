const loginHandler = require('./handleLogin')
const deleteHandler = require('./handleDelete')
const updateHandler = require('./handleUpdate')
const registerHandler = require('./handleRegister')

module.exports = {
    loginHandler,
    deleteHandler,
    updateHandler,
    registerHandler
}